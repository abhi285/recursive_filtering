-- macros/recursive_filter.sql
{% macro recursive_filter() %}
    -- This macro acts as a simple placeholder for direct SQL queries on the pre-filtered data.
    -- The actual filtering logic is applied upstream in `int_filtered_fact_cruise_revenue.sql`.
    TRUE
{% endmacro %}