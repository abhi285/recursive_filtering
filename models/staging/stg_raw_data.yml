# models/staging/stg_raw_data.yml
version: 2

sources:
  - name: nclh_raw_data
    database: DBT_TEST_DB # Matches the database created in Snowflake
    schema: CUSTOMER_DATA_MODEL # Matches the schema created in Snowflake
    tables:
      - name: RAW_CRUISE_REVENUE
      - name: RAW_CRUISE_ORGANIZATION